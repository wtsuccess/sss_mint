import Navbar from "../components/navbar"
import Minting from "../views/minting";

const Layout = () => {
    return (
        <>
            <Navbar></Navbar>
            <Minting></Minting>
        </>
    );
}

export default Layout;